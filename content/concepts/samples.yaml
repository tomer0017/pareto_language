# Concept Layer — SAMPLE concepts (Sprint 3 foundation proof; NOT production content).
# `exit` and `where` graduated to the production corpus (core-corpus.yaml) — the Core Corpus
# owns them canonically now. Only the Never-Teach demo remains here (a scored rejection record).
concepts:
  - id: concept.word.carburetor
    kind: word
    gloss: { en: "carburetor", he: "קרבורטור" }
    categories: [never-teach-demo]
    rof: 1
    layer: 4
    skillTarget: recognize
    neverTeach: true            # translator-cheap: kept as a scored rejection record
    rolScore: 2
    realizations: {}
